[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback: client-side routes such as /resume must serve index.html
# instead of 404ing on a direct visit or refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
